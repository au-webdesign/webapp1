let userName = "Pete"
function showMessage( msg="This is a recording ... " ){
    var message = msg + userName;
    alert( "msg=" + message )
    while( true ){
        let i = 100
        break
    }
    //alert( "i=" +i )
}
showMessage( )
alert( "bottom mesge" + message )
//showMessage( "Hey There:" )
